# Minimal dependencies for ML tests (no Docker/K8s/Flask).
#
# Versions are loose so pip can pick a wheel matching the local Python
# (3.10..3.13 are all supported by these libraries via different minor
# versions).
#
# IMPORTANT: pip installs the CPU build of torch by default.
# After `pip install -r requirements-tests.txt`, you MUST install the GPU
# build on top:
#
#   CUDA 12.1:
#     pip install --force-reinstall "torch>=2.1,<2.5" ^
#         --index-url https://download.pytorch.org/whl/cu121
#
#   CUDA 11.8:
#     pip install --force-reinstall "torch>=2.1,<2.5" ^
#         --index-url https://download.pytorch.org/whl/cu118
#
# Verify: python -c "import torch; print(torch.cuda.is_available())"
# See RUN_TESTS_ON_ANOTHER_PC.md for details.
numpy>=1.26,<3.0
scipy>=1.11,<2.0
pandas>=2.1,<3.0
statsmodels>=0.14,<0.16
torch>=2.1,<2.5
scikit-learn>=1.4,<2.0
pytest>=8.0,<9.0
pytest-html>=4.0,<5.0
pyyaml>=6.0,<7.0
